[Tails Server]
host=localhost
port=${HOST_PORT:-8808}

[Node Pool]
name=${INDY_POOL_NAME:-pool1}
genesis.txn.path=${HOME}/.indy_client/pool/pool1/pool1.txn

[VON Anchor]
seed=${TAILS_SERVER_SEED:-00000000000000000000TailsServer1}
wallet.name=tails-server
wallet.type=
wallet.key=
