[Tails Server]
host=localhost
port=${HOST_PORT}

[Node Pool]
name=pool1
genesis.txn.path=/path/to/genesis.txn

[VON Anchor]
seed=${TAILS_SERVER_SEED}
wallet.name=tails-server
wallet.type=
wallet.key=
