[Tails Server]
host=localhost
port=${HOST_PORT:-8808}

[Tails Client]
profile=issuer
tails.dir=${HOME}/.indy_client/tails

[Node Pool]
name=${INDY_POOL_NAME:-pool1}
genesis.txn.path=${HOME}/.indy_client/pool/pool1/pool1.txn

[VON Anchor]
seed=BC-Registrar-Anchor-000000000000
wallet.name=bc-registrar
wallet.type=
wallet.key=
